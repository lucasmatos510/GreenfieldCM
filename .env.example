# 🔧 Configurações do Sistema de Banco de Horas para Render

# Flask Settings
FLASK_ENV=production
FLASK_APP=app.py
SECRET_KEY=your-super-secret-key-here-change-this-in-production

# Database Configuration (Render PostgreSQL)
DATABASE_URL=postgresql://username:password@host:port/database_name

# Security Settings
WTF_CSRF_ENABLED=true
WTF_CSRF_TIME_LIMIT=3600

# Application Settings
TIMEZONE=America/Sao_Paulo
MAX_CONTENT_LENGTH=16777216

# Email Configuration (opcional para futuras notificações)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Debug Settings (sempre False em produção)
DEBUG=false
TESTING=false

# CORS Settings (se necessário para API futura)
CORS_ORIGINS=*

# Upload Settings
UPLOAD_FOLDER=uploads
MAX_FILE_SIZE=5242880

# Logging
LOG_LEVEL=INFO
LOG_FILE=app.log

# Cache Settings (para otimização futura)
CACHE_TYPE=simple
CACHE_DEFAULT_TIMEOUT=300

# Render Specific
PORT=10000
HOST=0.0.0.0

# Notas para Configuração no Render:
# 1. SECRET_KEY será gerado automaticamente pelo Render
# 2. DATABASE_URL será fornecido automaticamente pelo PostgreSQL do Render
# 3. Defina FLASK_ENV=production nas variáveis de ambiente do Render
# 4. Todas as outras variáveis são opcionais para funcionalidade básica